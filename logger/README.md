# Полиморфизм на примере синглтона Logger

Компиляция (С++17 для static inline поля)

```sh
g++ -std=c++17 main.cpp -o logger -lpthread
```

Для просмотра системых логов в Linux:

```sh
sudo journalctl -t CppLoggerApp
```